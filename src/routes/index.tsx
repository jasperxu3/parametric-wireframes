import { createFileRoute } from '@tanstack/react-router'
import { WireframeWorkbench } from '../components/wireframe-workbench'

export const Route = createFileRoute('/')({ component: WireframeWorkbench })
